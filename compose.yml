services:
  mini-nikki:
    build: .
    networks:
      - caddy_services
    volumes:
      - ./config.toml:/app/config.toml:ro
      - data:/data
    restart: unless-stopped

volumes:
  data:

networks:
  caddy_services:
    external: true
